
myfunction.cpp //不能把这个c文件加到项目中，否则编译过不了，报错有重复定义 ？？？未解决

按键继续不能用getrawlastkeypressed  否则起不到暂停作用

to_string(int)不行   要long long 或者long double , 做个类型转换就行了

-------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------
代码中仍有大部分可以优化的地方，很多处都存在冗余，可以进一步写函数
部分变量可能没使用，或者使用了也没意义